spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/tripmate}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres@123}
    driver-class-name: org.postgresql.Driver
